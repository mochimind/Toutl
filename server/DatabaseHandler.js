exports.createMessage = function(poster, message) {
	// check the poster name is valid
	
	// check the poster message is valid
	
	// check the poster parent id is valid
	
	// upload to database
	
	// return id of new entry
};

exports.loadView = function(id) {
	// check the id is valid
	
	// fetch all children in database
	
	// return in array
};